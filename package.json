{
  "name": "sigfox-platform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "scripts": {
    "setup": "cd webapp/ && npm install && ng build --environment prod && cd .. && cd api/ && npm install && node server/server.js"
  }
}
