import {ClickCopyDirective} from './click-copy.directive';

describe('ClickCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickCopyDirective();
    expect(directive).toBeTruthy();
  });
});
